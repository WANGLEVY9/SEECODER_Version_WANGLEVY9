# SEECODER demo fixture

This small project intentionally contains one defect. A suitable agent task is:

> Inspect this workspace, fix the failing tag-normalization behavior, run the test suite, and summarize the change.

Run the fixture test manually with:

```bash
python -m unittest discover -s tests -v
```
